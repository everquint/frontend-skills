---
"frontend-skills": patch
---

starter/AGENTS.md and starter/CLAUDE.md are reworded in plain language so both humans and agents read them without confusion: "already committed — install nothing" becomes "a full copy of the standard lives inside this repo — nothing needs to be installed or fetched", and the "vendored" jargon is dropped from the two front-door files (it stays in the reviewer agents, /pre-pr, and CI, where it is introduced with context).
